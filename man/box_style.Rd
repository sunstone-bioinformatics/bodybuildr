% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/styles.R
\name{box_style}
\alias{box_style}
\title{Define a box style}
\usage{
box_style(
  radius = unit(8, "pt"),
  border_color = "#D1D5DB",
  border_lwd = 1,
  fill = NA,
  margin = unit(c(0, 0, 0, 0), "pt"),
  margin_fill = NA,
  padding = unit(c(0, 0, 0, 0), "pt")
)
}
\arguments{
\item{radius}{Corner radius as `grid::unit`.}

\item{border_color}{Border color.}

\item{border_lwd}{Border line width (non-negative).}

\item{fill}{Fill color.}

\item{margin}{Outer margin as `grid::unit` (t, r, b, l).}

\item{margin_fill}{Fill color for the margin band (defaults to NA/transparent).}

\item{padding}{Inner padding as `grid::unit` (t, r, b, l).}
}
\value{
A list with box styling parameters.
}
\description{
Provides a reusable set of parameters for rounded boxes (radius, border, fill, margin, padding).
}
\details{
All unit arguments must be non-negative.

Margin layers:
- Box margin (`margin`, `margin_fill`) creates an inner gap around the box itself. Leave at zero for a single-layer look, or set `margin`/`margin_fill` for a double-layer/3D effect.
- Row/column lanes come from layout styles (e.g., [column_layout_style()], [subtitle_layout_style()]); they sit outside the box entirely.
}
