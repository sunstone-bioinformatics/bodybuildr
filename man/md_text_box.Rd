% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{md_text_box}
\alias{md_text_box}
\title{Construct a Markdown-formatted text box item}
\usage{
md_text_box(
  label,
  text_style = NULL,
  box_style = NULL,
  bg = NULL,
  pad_x = NULL,
  pad_y = NULL,
  link_color = "#2563EB"
)
}
\arguments{
\item{label}{Markdown-formatted character string.}

\item{text_style}{A `gpar` from [text_style()].}

\item{box_style}{A list from [box_style()].}

\item{bg}{Optional column background color override.}

\item{pad_x, pad_y}{Optional column padding overrides as `grid::unit` (non-negative).}

\item{link_color}{Color for hyperlink text. Default `"#2563EB"`.}
}
\value{
An object of class `"bbdr_md_text_box"` to use as an item in layout rows.
}
\description{
Like [text_box()], but `label` is parsed as Markdown and rendered as rich
HTML via `gridtext`. Supports **bold**, *italic*, `inline code`, line breaks,
and bullet lists. Links render as styled text but are not clickable in PDF.
}
\details{
Requires the `commonmark` package. If not installed, falls back to plain text.
}
